import React from "react";

export function LoginForm() {
    return (
        <form>
            <label htmlFor="input">Login Form</label>
            <input type="text" />
            <input type="text" />
            <button type="submit">Log In</button>
        </form>
    );
}